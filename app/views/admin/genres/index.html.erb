<h1>ジャンル一覧ページ</h1>

<%=form_with model: @genre, url:admin_genres_path do |f| %>
  <%=f.label :name, "ジャンル名" %>
  <%=f.text_field :name %>
  <%= f.submit "新規登録" %>
<%end%>

<% @genres.each do |genre| %>
  <p><%= genre.name%></p>
<%end%>